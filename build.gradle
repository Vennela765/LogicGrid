buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2'  // Updated for better compatibility with Gradle 8.0
    }
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}